COIN.MODULES.Teamoverview = function(sandbox) {
	// Public interface
	var module = {
		init: function() {
			
		},
		
		destroy: function() {
			
		}
	};
	
	// Private library (through closure)
	var library = {
			
	};

	// Return the public interface
	return module;
};